# FRONTEND_ISSUE_LOG.md

| # | Description | File:Line | Risk | Fixed? |
|---|-------------|-----------|------|--------|
| 1 | **Duplicate `downloadCsv` implementations**: `csvCore.ts` and `csvExport.ts` both export `downloadCsv`, `escapeCsvField`, and `toCsv` — identical copies. Risk of divergence on edits. | `csvCore.ts:33`, `csvExport.ts:37` | M | No |
| 2 | **Duplicate balance analysis functions**: `balanceAnalysis.ts` and `csvExport.ts` both define `buildDailyBalances`, `buildTrueBalances`, `calculateDailyCashFlows`, `calculateMonthlyCashFlows`, `getWorkDaysInMonth`, `findRepeatingTransactions`. Same logic duplicated. | `balanceAnalysis.ts:8,25,44,67,86,100`, `csvExport.ts:100,117,137,162,181,227` | H | No |
| 3 | **Duplicate `exportData` dispatcher**: Two separate `exportData` functions exist — one in `export/index.ts` (115 lines) and one in `csvExport.ts` (881 lines). The one in `csvExport.ts` is the actual large implementation; `export/index.ts` is a thin wrapper around `formatters/`. | `export/index.ts:67`, `csvExport.ts:828` | M | No |
| 4 | **True Revenue hardcoded at 0.9 multiplier**: `exportStatementsSummary` and `exportRevenueStatistics` use hardcoded `0.9` (90%) for true revenue with no explanation or configuration. | `csvExport.ts:55`, `csvExport.ts:682` | M | No |
| 5 | **Hardcoded "MCA Provider" string**: In multiple export formatters, lender name is hardcoded as literal string `'MCA Provider'` instead of actual provider data. | `csvExport.ts:485`, `balances.ts:85` | M | No |
| 6 | **`parseTransactionsFromMarkdown` import chain unclear**: `transactionParser.ts` re-exports from `./parsing/` but the actual parser logic path is unclear from the barrel — components import via multiple levels of indirection. | `transactionParser.ts:2-17`, `useInsightsCalculations.ts:4` | L | No |
| 7 | **`InsightsCalculations` interface not exported**: `useInsightsCalculations.ts` defines `InsightsCalculations` interface internally but it's not in the types index — other components cannot type-check against it cleanly. | `useInsightsCalculations.ts:26-33` | L | No |
| 8 | **`ExportConfig.revenueStats` missing `highestMonth`/`lowestMonth`**: The `ExportConfig` interface in `export/index.ts` has a simplified `revenueStats` that omits `highestMonth`/`lowestMonth` which are computed in `useInsightsCalculations`. | `export/index.ts:41-46` | L | No |
| 9 | **No error boundary around analysis components**: `AnalysisDetailed` and `McaFindings` will throw if `result` is malformed — no defensive null checks at component level (only inside render). | `AnalysisDetailed.tsx:9`, `McaFindings.tsx:10` | M | No |
| 10 | **`consecutiveErrorsRef` logic has side-effect order issue**: `consecutiveErrorsRef.current = 0` is set at Promise creation time, but the ref is also mutated inside the `setInterval` callback — the ref initialization and reset logic are separated by async boundaries. | `useExtractionPolling.ts:17,53` | L | No |
| 11 | **TypeScript: `BatchEntry` defined twice**: `BatchEntry` is defined in both `useExtractionState.ts` (line 15) and `ExtractionContext.tsx` (line 4), and re-exported from `extraction.ts`. Risk of type drift. | `useExtractionState.ts:15`, `ExtractionContext.tsx:4` | L | No |
| 12 | **`ThemeProvider` imported but source unknown**: `DashboardPage` imports `ThemeProvider` from `'../hooks/useTheme'` but this file was not in the glob scope for hooks — may not exist or may be incomplete. | `DashboardPage.tsx:2` | H | No |
| 13 | **`ReviewModal` lazy loaded with barrel-only import**: `ReviewModal` is lazy-loaded via `lazy(() => import('../components/ReviewModal'))` but `../components/index` only re-exports named components — default export from `ReviewModal` must be verified. | `DashboardPage.tsx:15` | L | No |
| 14 | **No loading state for batch extraction completion**: `useExtraction.ts` sets `status: 'complete'` immediately after last file processes, but doesn't wait for all batch results to be visually confirmed before marking done. | `useExtraction.ts:139-144` | L | No |
| 15 | **Hardcoded polling interval of 1500ms**: Polling interval is hardcoded; if the queue worker is slow (large PDF), 1500ms may be too aggressive or too conservative — not configurable. | `useExtractionPolling.ts:77` | L | No |
| 16 | **`localStorage` user stored without expiry/monitoring**: User session stored in `localStorage` by `LoginPage` with no expiry check, no token refresh, and no monitoring for tampering. | `LoginPage.tsx:29` | M | No |
| 17 | **CSV field escape uses double-quote doubling but not RFC 4180 length check**: The `escapeCsvField` implementation quotes fields containing comma/quote/newline but doesn't validate field length limits per RFC 4180. | `csvCore.ts:7-14`, `csvExport.ts:11-18` | L | No |
| 18 | **No PII masking in exported CSV data**: `exportMcaTransactions` and similar functions export transaction descriptions directly — if PII was detected and not fully scrubbed from descriptions before this point, it would be exported. | `csvExport.ts:75-79` | H | No |
| 19 | **`autoTag` uses regex on raw description**: `autoTag` in `transactionParser.ts` runs case-insensitive regex tests on unsanitized description strings — potential ReDoS if descriptions are attacker-controlled. | `transactionParser.ts:31` | M | No |
| 20 | **`findRepeatingTransactions` uses `Math.abs` on amount comparison**: When grouping by amount, uses `getAmount(tx)` which returns absolute value — so a +$500 credit and -$500 debit would be grouped as "repeating" incorrectly. | `balanceAnalysis.ts:106` | M | No |
